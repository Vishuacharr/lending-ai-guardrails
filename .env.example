# No API keys required — all evaluation is rule-based.
# Copy to .env and configure if you add LLM integrations in the future.

# Optional: Override routing thresholds
# EVAL_HIGH_THRESHOLD=0.85
# EVAL_LOW_THRESHOLD=0.60

# Optional: Audit log location
# AUDIT_LOG_PATH=sample_output/eval_report.jsonl
